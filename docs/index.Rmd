---
title: "SBCK Watershed Brigade Dashboard"
output:
  flexdashboard::flex_dashboard
---

### Example chart

The current time is `r Sys.time()`

```{r}
library(tidyverse)
mtcars %>%
  ggplot(aes(x = cyl,
             y = mpg)) +
  geom_point()
  
```

